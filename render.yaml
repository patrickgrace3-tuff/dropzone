services:
  - type: web
    name: dropzone-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: dropzone-db
          property: connectionString
      - key: FRONTEND_URL
        value: https://dropzone-1-3kev.onrender.com

  - type: web
    name: dropzone-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: dropzone-db
    databaseName: dropzone
    user: dropzone
    plan: free
